// ChatIcon is defined in the chat UI component
export { ChatIcon } from '../ui/chat/chat';

// ChatTab has no implementation file counterpart, keep it here
export enum ChatTab {
  Local = 0,
  Global = 1,
  Group = 2,
  System = 3,
}
